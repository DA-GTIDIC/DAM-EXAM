És possible crear una nova categoria sense fer cap modificació a l'API. Justifica la resposta.

Seria possible si el Enum s'hagues fet al Android, pero com esta a la API, per crear una categoria s ha
de modificar. Pertant no es possible.

Quins avantatges té fer anar l'API vs no fer-ho, per mostrar el nº de preguntes creades per cada usuari?

Si tens les preguntes amb el seu id, y els usuaris amb el seu id, nomes amb relacionar aquestes dos taules 
a la APi ya ho tindries.

En cambi si no vols treballar la api, hauries de guardar els valors del usuari al android, totes les preguntes
y fer una relacio o una logica al android.

Per la meva experiencia el treballar amb una Api es una manera mes facil de treballar amb gran informacio y
tenin una base de dades que no sense API.